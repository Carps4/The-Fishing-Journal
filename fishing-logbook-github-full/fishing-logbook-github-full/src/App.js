import React from 'react';

export default function App() {
  return (
    <div className="app">
      <header>
        <h1>Fishing Logbook</h1>
        <p>This is the source placeholder. The production-ready static build is located in <code>/dist</code>.</p>
      </header>
      <main>
        <p>To use the provided static build, deploy the files in the <code>/dist</code> folder to your static host (GitHub Pages, Netlify, Vercel, etc.).</p>
      </main>
    </div>
  );
}